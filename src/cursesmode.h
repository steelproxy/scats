#ifndef CURSESMODE_H
#define CURSESMODE_H

#include <curses.h>
#include <vector>
#include <sstream>

/*class CursesMode
{
public:
    CursesMode();
    virutal ~CursesMode(); // destruct

    string ReadCmd();
    void Clear();


private:
    typedef std::vector<std::string> HistoryCmd;

    HistoryCmd _history;

    WINDOW *_win;
};
*/
#endif